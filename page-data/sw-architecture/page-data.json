{"componentChunkName":"component---src-templates-blog-post-js","path":"/sw-architecture/","result":{"data":{"site":{"siteMetadata":{"title":"Mingyu's Blog"}},"markdownRemark":{"id":"4b2af1d3-8792-58f2-859a-d1e2f6766d20","excerpt":"소프트웨어 기획 설계 — 요구사항 정의부터 API 명세까지 0. 전체 흐름과 각 단계의 역할 소프트웨어 설계는 다음 순서로 진행된다. 각 단계는 이전 단계의 결과물을 입력으로 받는다. 순서가 중요한 이유는 앞 단계의 결정이 뒤 단계를 제약하기 때문이다. 화면에서 필요한 데이터를 DB…","html":"<h1>소프트웨어 기획 설계 — 요구사항 정의부터 API 명세까지</h1>\n<hr>\n<h2>0. 전체 흐름과 각 단계의 역할</h2>\n<p>소프트웨어 설계는 다음 순서로 진행된다. 각 단계는 이전 단계의 결과물을 입력으로 받는다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">요구사항 정의서\n  \"무엇을 만들 것인가\"\n        │\n        ▼\n화면설계서\n  \"사용자는 어떻게 사용하는가\"\n        │\n        ▼\nDB 스키마\n  \"데이터를 어떻게 저장할 것인가\"\n        │\n        ▼\nAPI 명세서\n  \"프론트엔드와 백엔드는 어떻게 통신할 것인가\"</code></pre></div>\n<p>순서가 중요한 이유는 앞 단계의 결정이 뒤 단계를 제약하기 때문이다. 화면에서 필요한 데이터를 DB에 저장하지 않으면 API가 줄 수 없고, 요구사항에 없는 기능을 화면에 넣으면 범위가 흔들린다. 반대 방향으로 설계하거나 단계를 건너뛰면 나중에 큰 수정 비용이 발생한다.</p>\n<hr>\n<h2>1. 요구사항 정의서</h2>\n<h3>핵심 목적</h3>\n<p>요구사항 정의서는 <strong>\"이 시스템이 무엇을 해야 하는가\"를 팀 전체가 동의하는 형태로 기록</strong>하는 문서다. 개발을 시작하기 전에 기획자·개발자·QA가 같은 기준을 가지게 한다.</p>\n<p>요구사항이 명확하지 않으면 개발이 진행될수록 \"그게 원래 있어야 했나요?\" 같은 질문이 반복되고, 뒤늦게 기능을 추가하면서 설계가 뒤틀린다.</p>\n<h3>기능 요구사항 vs 비기능 요구사항</h3>\n<p>많은 사람들이 기능 요구사항만 생각하고 비기능 요구사항을 빠뜨린다. 둘의 차이는 다음과 같다.</p>\n<p><strong>기능 요구사항</strong>: 시스템이 <strong>무엇을 해야 하는가</strong></p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">사용자는 이메일과 비밀번호로 로그인할 수 있다.\n기업을 검색하면 기업명과 종목코드를 확인할 수 있다.</code></pre></div>\n<p><strong>비기능 요구사항</strong>: 시스템이 <strong>어떻게 동작해야 하는가</strong></p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">일반 화면은 3초 이내에 응답해야 한다. (성능)\n비밀번호는 단방향 해시로 저장해야 한다. (보안)\nChrome, Edge, Safari 최신 버전을 지원해야 한다. (호환성)</code></pre></div>\n<p>비기능 요구사항은 개발 막바지에 \"아, 이것도 해야 하나?\"로 발견되면 구조를 바꿔야 하는 경우가 생긴다. <strong>보안·성능·호환성은 처음부터 명시해야 한다.</strong></p>\n<h3>MoSCoW 우선순위</h3>\n<p>모든 기능을 같은 비중으로 다루면 일정이 무너진다. 각 기능에 우선순위를 부여해야 한다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">Must   — 없으면 서비스 자체가 불가능한 기능\nShould — 있어야 하지만, 없어도 서비스는 동작하는 기능\nCould  — 있으면 좋지만 우선순위가 낮은 기능\nWon't  — 이번 버전에서는 하지 않는 기능</code></pre></div>\n<p><strong>흔한 실수</strong>: 모든 기능을 Must로 정의하는 것. Must가 너무 많으면 사실상 우선순위가 없는 것과 같다. MVP(최소 기능 제품) 범위를 먼저 확정하고, 나머지를 Should·Could로 분류하는 것이 현실적인 개발 계획의 출발점이다.</p>\n<h3>요구사항을 잘 쓰는 기준</h3>\n<p>좋은 요구사항은 <strong>측정 가능하고 검증 가능</strong>해야 한다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">❌ 나쁜 예: 빠르게 응답해야 한다.\n✅ 좋은 예: 정상 네트워크에서 요청 후 3초 이내에 주요 콘텐츠를 표시해야 한다.\n\n❌ 나쁜 예: 안전하게 저장해야 한다.\n✅ 좋은 예: 비밀번호는 bcrypt 또는 Argon2id 방식으로 해시하여 저장해야 한다.</code></pre></div>\n<p>\"빠르게\", \"안전하게\", \"쉽게\" 같은 표현은 나중에 완료 기준을 만들 수 없다. 요구사항은 QA가 테스트 케이스로 변환할 수 있는 형태여야 한다.</p>\n<h3>ID 체계와 추적성</h3>\n<p>요구사항이 많아지면 \"이 화면은 어떤 요구사항을 구현하는가?\", \"이 요구사항은 어디에서 확인하는가?\"를 추적하기 어려워진다. 처음부터 ID 체계를 정의하면 이 연결을 유지할 수 있다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">기능 요구사항:   REQ-FUNC-001\n비기능 요구사항: REQ-NFN-001\n화면:           SCR-AUTH-001</code></pre></div>\n<p>추적성 매트릭스(Traceability Matrix)는 요구사항과 화면·API가 양방향으로 연결되어 있는지 확인하는 표다. \"이 화면을 빼면 어떤 요구사항이 구현되지 않는가?\"를 즉시 파악할 수 있어야 한다.</p>\n<hr>\n<h2>2. 화면설계서</h2>\n<h3>핵심 목적</h3>\n<p>화면설계서는 <strong>사용자가 어떤 경험을 하게 되는가</strong>를 정의한다. 기능 요구사항을 사용자의 행동 흐름으로 변환하는 단계다. 프론트엔드 개발자는 이 문서를 기준으로 UI를 구현하고, 백엔드 개발자는 어떤 API가 필요한지 파악한다.</p>\n<h3>화면 목록을 먼저 정의한다</h3>\n<p>설계를 시작하기 전에 <strong>어떤 화면이 있고 어떻게 연결되는지</strong> 전체 지도를 그린다. 화면 하나씩 설계하기 전에 전체 구조를 먼저 파악해야 누락되는 화면이 없다.</p>\n<p>각 화면에는 ID, 이름, 서비스 내 위치(Depth), 주요 목적을 정의한다. Depth를 명확히 하면 화면 간 이동 방향이 자연스럽게 정해진다.</p>\n<h3>화면 디스크립션의 구성 요소</h3>\n<p>와이어프레임만으로는 \"이 버튼을 누르면 어떻게 되는가\"를 설명하기 부족하다. 각 화면은 다음 항목으로 설명되어야 한다.</p>\n<p><strong>화면 구성</strong>: 화면에 존재하는 UI 요소를 번호와 함께 나열한다. 번호는 사용자 동작·오류 문구 설명에서 참조할 때 사용한다.</p>\n<p><strong>사용자 동작</strong>: 사용자가 어떤 행동을 하면 어떤 일이 일어나는지 순서대로 서술한다. 이 흐름이 나중에 API 호출 순서가 된다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">검색창에 기업명 입력\n  → 검색 API 호출\n  → 검색 결과에서 기업 선택\n  → 리서치 시작 버튼 클릭\n  → AI Research 생성 API 호출\n  → 성공 시 researchId를 기준으로 다음 화면으로 이동</code></pre></div>\n<p><strong>검증</strong>: 입력값에 어떤 규칙이 적용되는지 명시한다. 클라이언트 검증과 서버 검증을 구분하는 것이 중요하다.</p>\n<blockquote>\n<p>클라이언트 검증은 UX를 위한 것이고, 서버 검증은 보안을 위한 것이다. 클라이언트 검증을 통과해도 서버에서 반드시 재검증해야 한다. 브라우저 개발자 도구로 클라이언트 검증은 언제든 우회할 수 있기 때문이다.</p>\n</blockquote>\n<p><strong>오류 문구</strong>: 발생 가능한 모든 오류 상황과 사용자에게 보여줄 메시지를 미리 정의한다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">이메일 미입력    → \"이메일을 입력해주세요.\"\n이메일 형식 오류 → \"올바른 이메일 형식을 입력해주세요.\"\n인증 실패        → \"이메일 또는 비밀번호가 올바르지 않습니다.\"</code></pre></div>\n<p>오류 메시지를 미리 정의하지 않으면 개발자마다 다른 문구를 쓰게 된다. 특히 <strong>보안 관점에서 오류 메시지가 불필요한 정보를 노출하면 안 된다.</strong> 예를 들어 \"존재하지 않는 계정입니다\"는 공격자에게 계정 존재 여부를 알려준다. 인증 실패 메시지는 이메일과 비밀번호 불일치를 구분하지 않고 통일하는 것이 좋다.</p>\n<p><strong>인수 조건 (Acceptance Criteria)</strong>: 이 화면이 완성됐다고 판단하는 기준을 구체적으로 명시한다. QA 테스트 케이스의 기준이 된다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">AC-001: 이메일을 입력하지 않은 상태에서는 로그인 버튼을 누를 수 없다.\nAC-002: 올바른 이메일과 비밀번호로 로그인하면 메인 화면으로 이동한다.\nAC-003: API 요청 중에는 로그인 버튼이 비활성화되어 중복 클릭이 방지된다.</code></pre></div>\n<h3>예외 처리 정책 표</h3>\n<p>화면의 예외 상황을 표로 정리하면 개발·QA 모두 명확한 기준이 생긴다.</p>\n<table>\n<thead>\n<tr>\n<th>상태</th>\n<th>발생 조건</th>\n<th>처리 방식</th>\n<th>HTTP 코드</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>정상</td>\n<td>인증 성공</td>\n<td>메인 화면으로 이동</td>\n<td>200</td>\n</tr>\n<tr>\n<td>잘못된 입력</td>\n<td>이메일 형식 오류</td>\n<td>필드 하단 오류 문구</td>\n<td>400</td>\n</tr>\n<tr>\n<td>인증 실패</td>\n<td>비밀번호 불일치</td>\n<td>인증 실패 메시지</td>\n<td>401</td>\n</tr>\n<tr>\n<td>로딩 중</td>\n<td>API 응답 대기</td>\n<td>버튼 비활성화 + Spinner</td>\n<td>-</td>\n</tr>\n<tr>\n<td>서버 오류</td>\n<td>내부 오류</td>\n<td>\"잠시 후 다시 시도해주세요.\"</td>\n<td>500</td>\n</tr>\n</tbody>\n</table>\n<h3>Happy Path만 설계하면 안 된다</h3>\n<p>화면설계서에서 가장 흔한 실수는 정상 흐름만 그리는 것이다. 실제 사용자는 네트워크가 끊기거나, 잘못된 값을 입력하거나, 다른 사용자의 데이터에 접근하려고 시도한다. <strong>빈 상태(Empty State), 로딩 상태, 오류 상태를 정상 흐름과 함께 설계해야 한다.</strong></p>\n<hr>\n<h2>3. DB 스키마 설계</h2>\n<h3>핵심 목적</h3>\n<p>DB 스키마는 <strong>서비스에서 다루는 데이터를 어떻게 저장하고 관계 지을 것인가</strong>를 결정한다. 화면설계서에서 표시해야 하는 데이터, API가 주고받을 데이터를 역추적해서 필요한 테이블과 컬럼을 정의한다.</p>\n<h3>도메인을 먼저 파악한다</h3>\n<p>테이블을 하나씩 설계하기 전에 서비스의 핵심 도메인 객체가 무엇인지 먼저 파악한다. 도메인이 명확해야 테이블 간의 경계가 자연스럽게 정해진다.</p>\n<p>예를 들어 AI 금융 리서치 서비스라면 사용자·기업·Research·수집 데이터·리포트·추가 질문이 핵심 도메인이다. 각 도메인이 어떤 관계를 맺는지 그림으로 정리한 다음 테이블 설계로 넘어가는 것이 좋다.</p>\n<h3>설계할 때 반드시 챙겨야 할 것들</h3>\n<p><strong>공통 컬럼 표준화</strong></p>\n<p>모든 테이블에 동일한 감사(Audit) 컬럼을 추가한다. 나중에 빠뜨린 테이블에 컬럼을 추가하면 ALTER TABLE 비용이 발생한다.</p>\n<div class=\"gatsby-highlight\" data-language=\"sql\"><pre class=\"language-sql\"><code class=\"language-sql\">created_at <span class=\"token keyword\">TIMESTAMP</span> <span class=\"token operator\">NOT</span> <span class=\"token boolean\">NULL</span> <span class=\"token keyword\">DEFAULT</span> <span class=\"token keyword\">CURRENT_TIMESTAMP</span>\nupdated_at <span class=\"token keyword\">TIMESTAMP</span> <span class=\"token operator\">NOT</span> <span class=\"token boolean\">NULL</span> <span class=\"token keyword\">DEFAULT</span> <span class=\"token keyword\">CURRENT_TIMESTAMP</span> <span class=\"token keyword\">ON</span> <span class=\"token keyword\">UPDATE</span> <span class=\"token keyword\">CURRENT_TIMESTAMP</span></code></pre></div>\n<p><strong>Soft Delete 설계</strong></p>\n<p>중요한 데이터는 물리적으로 삭제하지 않고 상태값으로 관리한다. 사용자 탈퇴 시 계정을 즉시 삭제하면 해당 사용자가 작성한 데이터의 참조 무결성이 깨질 수 있고, 감사 추적도 불가능해진다.</p>\n<div class=\"gatsby-highlight\" data-language=\"sql\"><pre class=\"language-sql\"><code class=\"language-sql\"><span class=\"token keyword\">status</span>     <span class=\"token keyword\">VARCHAR</span><span class=\"token punctuation\">(</span><span class=\"token number\">20</span><span class=\"token punctuation\">)</span> <span class=\"token operator\">NOT</span> <span class=\"token boolean\">NULL</span> <span class=\"token keyword\">DEFAULT</span> <span class=\"token string\">'ACTIVE'</span>  <span class=\"token comment\">-- ACTIVE / WITHDRAWN</span>\ndeleted_at <span class=\"token keyword\">TIMESTAMP</span> <span class=\"token boolean\">NULL</span>                          <span class=\"token comment\">-- 삭제 시점 기록</span></code></pre></div>\n<p><strong>상태값 설계</strong></p>\n<p>복잡한 프로세스는 상태값으로 전체 흐름을 표현한다. 상태 전이 다이어그램을 그려보면 어떤 상태가 필요한지, 어떤 전이가 허용되는지 명확해진다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">PENDING → RUNNING → COMPLETED\n                 ↘ FAILED</code></pre></div>\n<p>허용되지 않는 상태 전이(예: COMPLETED → RUNNING)를 애플리케이션 레벨에서 차단해야 한다.</p>\n<h3>관계 설계의 핵심 원칙</h3>\n<p><strong>M:N 관계는 반드시 중간 테이블로 해소한다</strong></p>\n<p>사용자가 여러 기업을 관심기업으로 등록하고, 한 기업은 여러 사용자의 관심기업이 될 수 있다면 M:N 관계다. 이는 반드시 중간 테이블로 표현해야 한다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">users ↔ companies  →  watchlists(user_id, company_id)</code></pre></div>\n<p>중간 테이블에 복합 유니크 제약을 걸면 중복 등록을 DB 레벨에서 방지할 수 있다.</p>\n<p><strong>1:1 관계에서 어느 테이블에 FK를 두는가</strong></p>\n<p>Research와 Report가 1:1 관계라면 Report 테이블이 Research ID를 FK로 가지는 것이 자연스럽다. Research는 Report가 없어도 존재할 수 있지만(진행 중), Report는 반드시 Research에 속하기 때문이다.</p>\n<p><strong>현재 데이터와 시점 데이터를 분리한다</strong></p>\n<p>예를 들어 기업의 현재 주가 데이터와 특정 분석 시점의 주가 데이터는 별도 테이블로 관리해야 한다. 현재 데이터만 있으면 과거 분석 결과를 나중에 조회할 때 수치가 달라진다. 금융 서비스에서 <strong>분석 기준시점 고정은 신뢰성의 핵심</strong>이다.</p>\n<h3>인덱스 전략</h3>\n<p>인덱스는 조회 성능을 높이지만 삽입·수정 성능을 낮춘다. 무조건 많이 걸면 오히려 성능이 나빠질 수 있다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">인덱스가 필요한 컬럼\n  - WHERE 조건에 자주 등장하는 컬럼 (ex. user_id, status)\n  - JOIN에 사용되는 FK 컬럼\n  - 정렬(ORDER BY)에 자주 사용하는 컬럼\n\n인덱스가 필요 없는 컬럼\n  - 카디널리티가 낮은 컬럼 (ex. BOOLEAN, 2~3개짜리 ENUM)\n  - 거의 조회하지 않는 컬럼</code></pre></div>\n<p>UNIQUE 제약을 걸면 자동으로 인덱스가 생성된다. 중복 방지를 위해 UNIQUE 제약을 건 컬럼에 별도 인덱스를 추가할 필요는 없다.</p>\n<hr>\n<h2>4. API 명세서</h2>\n<h3>핵심 목적</h3>\n<p>API 명세서는 <strong>프론트엔드와 백엔드가 어떻게 통신할 것인가에 대한 계약서</strong>다. 이 계약이 명확하면 프론트엔드와 백엔드가 병렬로 개발할 수 있다. 명세 없이 구두로 약속하면 개발 중에 계속 \"이거 어떻게 받아요?\"라는 질문이 생긴다.</p>\n<h3>REST API 설계 원칙</h3>\n<p>REST API는 리소스(Resource) 중심으로 URL을 설계하고, HTTP 메서드로 행위를 표현한다.</p>\n<p><strong>URL은 리소스(명사)로 표현한다</strong></p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">❌ /getCompanyList       → 동사 기반 (REST 원칙 위반)\n✅ /api/companies        → 리소스 기반\n\n❌ /createResearch\n✅ /api/research         → POST 메서드가 \"생성\" 의미를 담음\n\n❌ /api/user/1/getResearch\n✅ /api/users/me/research → me를 쓰면 userId를 URL에 노출하지 않아도 됨</code></pre></div>\n<p><strong>계층 관계는 URL 구조로 표현한다</strong></p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">/api/research/{id}/report          → 특정 Research의 리포트\n/api/research/{id}/report/pdf      → 리포트의 PDF 버전\n/api/research/{id}/questions       → 특정 Research의 추가 질문\n\n/api/users/me/favorites            → 내 관심기업 목록\n/api/users/me/favorites/{companyId} → 특정 관심기업</code></pre></div>\n<p>단, 계층이 너무 깊어지면 URL이 복잡해진다. 일반적으로 3단계 이상은 재검토를 권장한다.</p>\n<h3>HTTP 메서드와 멱등성</h3>\n<table>\n<thead>\n<tr>\n<th>메서드</th>\n<th>의미</th>\n<th>멱등성</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>GET</td>\n<td>조회</td>\n<td>✅ 여러 번 호출해도 결과 동일</td>\n</tr>\n<tr>\n<td>POST</td>\n<td>생성</td>\n<td>❌ 호출할 때마다 새 리소스 생성</td>\n</tr>\n<tr>\n<td>PUT</td>\n<td>전체 수정</td>\n<td>✅ 같은 요청 여러 번 = 같은 결과</td>\n</tr>\n<tr>\n<td>PATCH</td>\n<td>부분 수정</td>\n<td>❌ (구현에 따라 다름)</td>\n</tr>\n<tr>\n<td>DELETE</td>\n<td>삭제</td>\n<td>✅ 이미 삭제된 것을 다시 삭제해도 결과 동일</td>\n</tr>\n</tbody>\n</table>\n<p><strong>멱등성</strong>은 같은 요청을 여러 번 보내도 결과가 동일한 성질이다. 네트워크 오류로 요청이 재전송될 때 중요하다. POST는 멱등하지 않으므로, 네트워크 재시도 시 중복 생성이 일어날 수 있다. 이를 방지하기 위해 클라이언트가 Idempotency Key를 헤더에 포함하는 방식을 쓰기도 한다.</p>\n<h3>HTTP 상태 코드 선택 기준</h3>\n<p>상태 코드는 클라이언트가 응답을 파악하고 적절하게 처리하기 위한 신호다. 모든 응답을 200으로 처리하고 본문에 success: false를 담는 방식은 REST 원칙에 맞지 않는다.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">2xx 성공\n  200 OK          → 조회·수정 성공\n  201 Created     → 새 리소스 생성 (POST 성공)\n  204 No Content  → 성공이지만 반환 데이터 없음 (DELETE 성공)\n\n4xx 클라이언트 오류 (요청 자체가 잘못됨)\n  400 Bad Request → 잘못된 입력값, 형식 오류\n  401 Unauthorized → 인증 없음 또는 만료 → \"당신이 누구인지 모름\"\n  403 Forbidden    → 인증됐지만 권한 없음 → \"당신은 알지만 이건 안 됨\"\n  404 Not Found    → 요청한 리소스가 없음\n  409 Conflict     → 중복 충돌 (관심기업 중복 등록, 중복 요청)\n  429 Too Many Requests → Rate Limit 초과\n\n5xx 서버 오류 (서버가 처리에 실패)\n  500 Internal Server Error → 서버 내부 오류\n  504 Gateway Timeout → 외부 서비스 응답 초과</code></pre></div>\n<p><strong>401 vs 403을 헷갈리면 안 된다.</strong> 401은 로그인이 필요하다는 의미이고, 403은 로그인은 됐지만 해당 리소스에 접근할 권한이 없다는 의미다. 다른 사용자의 데이터에 접근하려 할 때는 403이 맞다.</p>\n<h3>응답 구조 통일</h3>\n<p>API마다 응답 형식이 다르면 프론트엔드에서 처리 로직이 제각각이 된다. 성공·실패·목록 응답의 구조를 프로젝트 전체에서 통일해야 한다.</p>\n<div class=\"gatsby-highlight\" data-language=\"json\"><pre class=\"language-json\"><code class=\"language-json\"><span class=\"token comment\">// 단건 성공 응답</span>\n<span class=\"token punctuation\">{</span> <span class=\"token property\">\"data\"</span><span class=\"token operator\">:</span> <span class=\"token punctuation\">{</span> ... <span class=\"token punctuation\">}</span> <span class=\"token punctuation\">}</span>\n\n<span class=\"token comment\">// 목록 성공 응답 (페이지네이션 포함)</span>\n<span class=\"token punctuation\">{</span>\n  <span class=\"token property\">\"data\"</span><span class=\"token operator\">:</span> <span class=\"token punctuation\">[</span>...<span class=\"token punctuation\">]</span><span class=\"token punctuation\">,</span>\n  <span class=\"token property\">\"page\"</span><span class=\"token operator\">:</span> <span class=\"token number\">0</span><span class=\"token punctuation\">,</span>\n  <span class=\"token property\">\"size\"</span><span class=\"token operator\">:</span> <span class=\"token number\">10</span><span class=\"token punctuation\">,</span>\n  <span class=\"token property\">\"totalElements\"</span><span class=\"token operator\">:</span> <span class=\"token number\">42</span><span class=\"token punctuation\">,</span>\n  <span class=\"token property\">\"totalPages\"</span><span class=\"token operator\">:</span> <span class=\"token number\">5</span>\n<span class=\"token punctuation\">}</span>\n\n<span class=\"token comment\">// 실패 응답</span>\n<span class=\"token punctuation\">{</span>\n  <span class=\"token property\">\"error\"</span><span class=\"token operator\">:</span> <span class=\"token punctuation\">{</span>\n    <span class=\"token property\">\"code\"</span><span class=\"token operator\">:</span> <span class=\"token string\">\"COMPANY_NOT_FOUND\"</span><span class=\"token punctuation\">,</span>\n    <span class=\"token property\">\"message\"</span><span class=\"token operator\">:</span> <span class=\"token string\">\"해당 기업 정보를 찾을 수 없습니다.\"</span><span class=\"token punctuation\">,</span>\n    <span class=\"token property\">\"status\"</span><span class=\"token operator\">:</span> <span class=\"token number\">404</span>\n  <span class=\"token punctuation\">}</span>\n<span class=\"token punctuation\">}</span></code></pre></div>\n<p>에러 코드(<code class=\"language-text\">COMPANY_NOT_FOUND</code>)를 별도로 정의하면 클라이언트가 메시지 문자열을 파싱하지 않고 코드 기반으로 분기 처리할 수 있다.</p>\n<h3>OpenAPI(Swagger) 명세 작성</h3>\n<p>API 명세는 자연어로 설명하는 것보다 <strong>OpenAPI 표준 형식으로 작성</strong>하는 것이 좋다. YAML로 작성하면 Swagger UI가 자동으로 시각화해주고, 프론트엔드 개발자가 직접 API를 테스트해볼 수 있다.</p>\n<div class=\"gatsby-highlight\" data-language=\"yaml\"><pre class=\"language-yaml\"><code class=\"language-yaml\"><span class=\"token key atrule\">/api/auth/login</span><span class=\"token punctuation\">:</span>\n  <span class=\"token key atrule\">post</span><span class=\"token punctuation\">:</span>\n    <span class=\"token key atrule\">tags</span><span class=\"token punctuation\">:</span> <span class=\"token punctuation\">[</span>Auth<span class=\"token punctuation\">]</span>\n    <span class=\"token key atrule\">summary</span><span class=\"token punctuation\">:</span> 로그인\n    <span class=\"token key atrule\">requestBody</span><span class=\"token punctuation\">:</span>\n      <span class=\"token key atrule\">required</span><span class=\"token punctuation\">:</span> <span class=\"token boolean important\">true</span>\n      <span class=\"token key atrule\">content</span><span class=\"token punctuation\">:</span>\n        <span class=\"token key atrule\">application/json</span><span class=\"token punctuation\">:</span>\n          <span class=\"token key atrule\">schema</span><span class=\"token punctuation\">:</span>\n            <span class=\"token key atrule\">type</span><span class=\"token punctuation\">:</span> object\n            <span class=\"token key atrule\">required</span><span class=\"token punctuation\">:</span> <span class=\"token punctuation\">[</span>email<span class=\"token punctuation\">,</span> password<span class=\"token punctuation\">]</span>\n            <span class=\"token key atrule\">properties</span><span class=\"token punctuation\">:</span>\n              <span class=\"token key atrule\">email</span><span class=\"token punctuation\">:</span>\n                <span class=\"token key atrule\">type</span><span class=\"token punctuation\">:</span> string\n                <span class=\"token key atrule\">format</span><span class=\"token punctuation\">:</span> email\n              <span class=\"token key atrule\">password</span><span class=\"token punctuation\">:</span>\n                <span class=\"token key atrule\">type</span><span class=\"token punctuation\">:</span> string\n                <span class=\"token key atrule\">minLength</span><span class=\"token punctuation\">:</span> <span class=\"token number\">8</span>\n    <span class=\"token key atrule\">responses</span><span class=\"token punctuation\">:</span>\n      <span class=\"token key atrule\">\"200\"</span><span class=\"token punctuation\">:</span>\n        <span class=\"token key atrule\">description</span><span class=\"token punctuation\">:</span> 로그인 성공\n        <span class=\"token key atrule\">content</span><span class=\"token punctuation\">:</span>\n          <span class=\"token key atrule\">application/json</span><span class=\"token punctuation\">:</span>\n            <span class=\"token key atrule\">schema</span><span class=\"token punctuation\">:</span>\n              <span class=\"token key atrule\">properties</span><span class=\"token punctuation\">:</span>\n                <span class=\"token key atrule\">accessToken</span><span class=\"token punctuation\">:</span>\n                  <span class=\"token key atrule\">type</span><span class=\"token punctuation\">:</span> string\n      <span class=\"token key atrule\">\"401\"</span><span class=\"token punctuation\">:</span>\n        <span class=\"token key atrule\">description</span><span class=\"token punctuation\">:</span> 이메일 또는 비밀번호 불일치</code></pre></div>\n<p>인증이 필요한 API는 <code class=\"language-text\">security</code> 필드로 명시한다. 이렇게 하면 Swagger UI에서 토큰을 입력해 인증이 필요한 API를 테스트할 수 있다.</p>\n<div class=\"gatsby-highlight\" data-language=\"yaml\"><pre class=\"language-yaml\"><code class=\"language-yaml\"><span class=\"token key atrule\">security</span><span class=\"token punctuation\">:</span>\n  <span class=\"token punctuation\">-</span> <span class=\"token key atrule\">bearerAuth</span><span class=\"token punctuation\">:</span> <span class=\"token punctuation\">[</span><span class=\"token punctuation\">]</span></code></pre></div>\n<h3>API 설계에서 보안을 챙겨야 하는 부분</h3>\n<p><strong><code class=\"language-text\">/me</code> 패턴 사용</strong></p>\n<p><code class=\"language-text\">/api/users/{userId}/research</code>처럼 userId를 URL에 노출하면 다른 사람의 userId를 넣어 데이터를 조회하려는 시도가 생긴다. <code class=\"language-text\">/api/users/me/research</code>처럼 me를 쓰면 서버가 토큰에서 userId를 추출하므로 더 안전하다.</p>\n<p><strong>클라이언트에서 받은 값을 그대로 신뢰하지 않는다</strong></p>\n<p>예를 들어 Research 생성 요청에 기업 ID가 포함되어 있더라도, 서버에서 해당 기업이 실제로 존재하는지 다시 검증해야 한다. 클라이언트는 얼마든지 임의의 값을 전송할 수 있다.</p>\n<p><strong>민감 정보는 응답에 포함하지 않는다</strong></p>\n<p>비밀번호 해시, API Key, 토큰 등은 응답 본문이나 에러 메시지에 포함되면 안 된다. 에러 로그에 포함되는 것도 주의해야 한다.</p>\n<hr>\n<h2>5. 단계 간 연결이 핵심이다</h2>\n<p>각 단계를 잘 작성하는 것도 중요하지만, <strong>단계 간 연결이 일관성 있게 유지되는 것이 더 중요하다.</strong></p>\n<p>화면에서 보여주는 데이터가 DB에 저장되어 있지 않으면 API가 줄 수 없다. 요구사항에 없는 기능이 화면에 들어가면 범위가 흔들린다. API 응답에 있는 필드가 화면에서 사용되지 않으면 불필요한 데이터를 전송하는 것이다.</p>\n<p>추적성(Traceability)을 유지한다는 것은 <strong>요구사항 ID가 화면에 연결되고, 화면이 API에 연결되고, API가 DB 테이블에 연결되는 흐름이 끊기지 않아야 한다</strong>는 의미다. 이 연결이 끊기는 지점이 구현 누락이나 설계 불일치가 발생하는 곳이다.</p>\n<hr>\n<h2>핵심 개념 한줄 요약</h2>\n<table>\n<thead>\n<tr>\n<th>개념</th>\n<th>한줄 요약</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>기능 요구사항</strong></td>\n<td>시스템이 무엇을 해야 하는가 — 측정·검증 가능하게</td>\n</tr>\n<tr>\n<td><strong>비기능 요구사항</strong></td>\n<td>시스템이 어떻게 동작해야 하는가 — 보안·성능·호환성</td>\n</tr>\n<tr>\n<td><strong>MoSCoW</strong></td>\n<td>기능 우선순위 결정 기법 — Must만 너무 많으면 의미 없음</td>\n</tr>\n<tr>\n<td><strong>인수 조건</strong></td>\n<td>완성 기준 — QA 테스트 케이스의 원본</td>\n</tr>\n<tr>\n<td><strong>클라이언트 검증 vs 서버 검증</strong></td>\n<td>클라이언트는 UX, 서버는 보안 — 둘 다 필요</td>\n</tr>\n<tr>\n<td><strong>Soft Delete</strong></td>\n<td>물리 삭제 대신 상태값으로 관리 — 감사 추적·참조 무결성</td>\n</tr>\n<tr>\n<td><strong>상태 전이</strong></td>\n<td>허용된 상태 변화만 가능하도록 애플리케이션에서 제어</td>\n</tr>\n<tr>\n<td><strong>M:N 중간 테이블</strong></td>\n<td>다대다 관계는 반드시 중간 테이블로 해소</td>\n</tr>\n<tr>\n<td><strong>멱등성</strong></td>\n<td>같은 요청 여러 번 = 같은 결과 — GET·PUT·DELETE는 멱등</td>\n</tr>\n<tr>\n<td><strong>401 vs 403</strong></td>\n<td>401은 인증 없음, 403은 권한 없음</td>\n</tr>\n<tr>\n<td><strong>응답 구조 통일</strong></td>\n<td>성공·실패·목록 응답 형식을 프로젝트 전체에서 통일</td>\n</tr>\n<tr>\n<td><strong>추적성</strong></td>\n<td>요구사항 → 화면 → API → DB가 끊기지 않고 연결되어야 함</td>\n</tr>\n</tbody>\n</table>","frontmatter":{"title":"소프트웨어 기획 설계","date":"August 21, 2026","description":"소프트웨어 프로젝트 기획 설계의 전체 흐름 정리. 요구사항 정의서 → 화면설계서 → DB 스키마 → API 명세로 이어지는 단계별 핵심 개념과 유의사항. 각 단계에서 무엇을 결정해야 하고, 어떤 실수가 흔히 발생하는지 중심으로 정리"}},"previous":{"fields":{"slug":"/vue/"},"frontmatter":{"title":"Vue.js 핵심 문법 정리"}},"next":null},"pageContext":{"id":"4b2af1d3-8792-58f2-859a-d1e2f6766d20","previousPostId":"9dbb510f-2d17-54ee-9d09-53d557a32485","nextPostId":null}},"staticQueryHashes":["2841359383","3257411868"],"slicesMap":{}}